<?xml version="1.0" encoding="UTF-8"?>

<beans>
    <!-- QAT User -->
    <bean id="qatUser1" class="Credential">
        <property name="username" value="${QAT.Credentials.Username1}" />
        <property name="password" value="${QAT.Credentials.Password}" />
    </bean>

    <bean id="qatUser2" class="com.demo.config.Credentials">
        <property name="username" value="${QAT.Credentials.Username2}" />
        <property name="password" value="${QAT.Credentials.Password}" />
    </bean>
</beans>